test:
  override:
    - python test/FakeAppTest.py

deployment:
  dockerhub:
    branch: master
    commands:
      - "curl -H \"Content-Type: application/json\" --data '{\"build\": true}' -X POST $DOCKER_BUILD_URL"